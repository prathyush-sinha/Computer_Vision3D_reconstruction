# Sample Outputs

Place recruiter-facing visual artifacts here after rerunning the pipeline locally, for example:

- `sparse_reconstruction.png`
- `camera_poses.png`
- `dense_point_cloud.png`
- `point_cloud.ply`

The original notebook contains embedded Plotly visualization output, but standalone image exports are preferred for GitHub review.
